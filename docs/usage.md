# Usage

*Stub — to be filled in after the first real weekend project session.*

This file is where the day-to-day patterns live: when to use Build vs Plan mode, how to switch models mid-session, when local is enough vs when to flip to LAN or Cloud, how to recover when a tool call loops.

## To be written

- First-session walkthrough: opening a fresh project, picking a model, running a real task
- Build vs Plan mode — when each makes sense
- Mid-session model switching via `/models` (and when it's worth the friction)
- Recognizing when the local model is out of its depth, and what to do next
- Error recovery: tool call loops, context overflow, model hallucinations
- "Offline mood" mode — flight, coffee shop, vacation
- Cost awareness when flipping to `:cloud` models

## Until this is written

Follow the happy path in `docs/install-macos.md` through Step 5 (`ollama launch opencode --model qwen3:8b`) and use the agent the same way you would use any other coding agent. The patterns that hold up will land here.
